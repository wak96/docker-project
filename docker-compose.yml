version: '3.8'

services:
  web:
    build: ./Walid-Ayaan-Kawsar-main  # ← Changed this line
    command: gunicorn --bind 0.0.0.0:8000 Portfolio.wsgi:application
    volumes:
      - ./Walid-Ayaan-Kawsar-main/static:/app/static
    expose:
      - "8000"
    depends_on:
      - db

  nginx:
    image: nginx:latest
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ./Walid-Ayaan-Kawsar-main/static:/app/static
    depends_on:
      - web

  db:
    image: postgres:13
    environment:
      - POSTGRES_DB=mydb
      - POSTGRES_USER=myuser
      - POSTGRES_PASSWORD=mypassword